<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >	
<mapper namespace="board.A03_Dao">
	<select id="boardList" resultType="board" parameterType="boardsch">
		select * 
		from board 
		WHERE 1=1
		AND subject LIKE '%'|| #{subject} ||'%'
		AND writer LIKE '%'|| #{writer} ||'%'
		order by regdte desc
	</select>
	
	<insert id="insertBoard" parameterType="board">
		insert into board values(board_seq.nextval,
		#{refno},#{subject},#{content},#{writer},0, sysdate, sysdate)
	</insert>
	<update id="uptReadCnt" parameterType="int">
		UPDATE board 
		SET readcnt = readcnt+1
		WHERE NO =#{no} 
	</update>
	<select id="getBoard" resultType="board" parameterType="int">
		SELECT * FROM board WHERE 
 		NO=#{no}
	</select>
	<select id="baseballList" resultType="BaseBallTeam">
		SELECT * FROM baseballteam
	</select>
	<insert id="insertBaseballTeam" parameterType="BaseBallTeam">
		INSERT INTO baseballteam VALUES(baseballteam_seq.nextval,
		#{teamname},#{ranking},#{hometown},#{player})
	</insert>
	
	<select id="BaseballTeamDetail" resultType="BaseBallTeam" parameterType="int">
		SELECT * FROM rentalInfo
		WHERE rentaltime = returntime
		AND returntime between '20230130' AND '20230208'
		AND id='himan'
	</select>
</mapper>		
		