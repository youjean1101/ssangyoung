/**
 * 
 */

  // 내용에 파일업로드 버튼 숨김처리
  $(".ql-link").hide()
  
  
  // $("div[data-id='2']").remove() // 태그 기본값 삭제(스크립트로 걸려있어서 js로 삭제 진행)
  // $("button[aria-label='Remove item: 'Two'']").remove()// 태그 기본값삭제버튼 삭제(스크립트로 걸려있어서 js로 삭제 진행)
  // $("div[data-id='2']").remove() //드랍다운 value값 수정하기
  